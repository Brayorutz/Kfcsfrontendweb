# TODO: Directors Portal Persistence Steps (Approved Plan)

## Progress:
- [x] Create TODO-persistence-steps.md
- [x] 1. Add loadDirectors(), saveDirectors() functions to server/routes.ts
- [x] 2. Add loadDirectorFiles(), saveDirectorFiles() functions  
- [x] 3. Load data on registerRoutes() start: if files exist, load/parse JSON
- [x] 4. Save after mutations: POST /accounts, POST /files, DELETE /accounts/:id, DELETE /files/:id
- [x] 5. Test: create account → restart server → account persists; same for files
- [x] 6. Update TODO complete, attempt_completion

