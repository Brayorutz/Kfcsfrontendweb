# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/kfcs

# Session Secret
SESSION_SECRET=your-session-secret-here

# SMTP Email Configuration
# Configure these values to enable email sending functionality
# For Gmail, use: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587
# You'll need to use an App Password if using Gmail (not your regular password)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=Kabianga Farmers Cooperative <noreply@yourdomain.com>

# Server Configuration
PORT=3000
NODE_ENV=development

